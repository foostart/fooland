<% include header %>
  <div class="col-md-4 offset-md-4 mb-5 mt-5">
    <div class="card">
      <div class="card-header" style="background-color: lightseagreen">
        Summary Report
      </div>
      <div class="card-body">
        <div class="alert alert-success">Tổng số trang BDS :
          <strong><%= totalSites %></strong>
        </div>
        <div class="alert alert-info">Tổng số dữ liệu lấy về :
          <strong><%= totalRealEstates%></strong>
        </div>
        <div class="alert alert-primary">Tổng số pattern:
          <strong><%= totalPatterns %></strong>
        </div>
      </div>
    </div>
  </div>
<% include footer %>